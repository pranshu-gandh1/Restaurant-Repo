name: restaurant
description: A new Flutter project.

publish_to: 'none' # Remove this line if you want to publish it

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.0.2  # Add logging package here
  provider: ^6.1.3 # Add the correct version of provider package

dev_dependencies:
  flutter_test:
    sdk: flutter
  
dev_dependencies:
  flutter_lints: ^2.0.0 # or whatever version is compatible

flutter:
  uses-material-design: true